<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petmall.mapper.ServiceMapper">

    <resultMap id="BaseResultMap" type="com.petmall.entity.Service">
        <id column="id" property="id"/>
        <result column="category_id" property="categoryId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="price" property="price"/>
        <result column="original_price" property="originalPrice"/>
        <result column="image_url" property="imageUrl"/>
        <result column="duration" property="duration"/>
        <result column="sales" property="sales"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM service WHERE id = #{id}
    </select>

    <select id="findByCategory" resultMap="BaseResultMap">
        SELECT * FROM service WHERE category_id = #{categoryId} AND status = 1
        ORDER BY sales DESC
    </select>

    <select id="findRecommend" resultMap="BaseResultMap">
        SELECT * FROM service WHERE status = 1
        ORDER BY sales DESC LIMIT 10
    </select>

</mapper>