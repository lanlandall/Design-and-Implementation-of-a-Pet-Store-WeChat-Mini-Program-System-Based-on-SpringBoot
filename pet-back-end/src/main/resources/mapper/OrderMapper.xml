<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petmall.mapper.OrderMapper">

    <resultMap id="OrderResultMap" type="com.petmall.entity.Orders">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="order_type" property="orderType"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="pay_amount" property="payAmount"/>
        <result column="status" property="status"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="receiver_address" property="receiverAddress"/>
        <result column="pay_time" property="payTime"/>
        <result column="ship_time" property="shipTime"/>
        <result column="complete_time" property="completeTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="OrderItemResultMap" type="com.petmall.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="service_id" property="serviceId"/>
        <result column="name" property="name"/>
        <result column="image_url" property="imageUrl"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="service_time" property="serviceTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findById" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="findByOrderNo" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>

    <select id="findByUserId" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <insert id="insertOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (order_no, user_id, order_type, total_amount, pay_amount,
        status, receiver_name, receiver_phone, receiver_address, create_time, update_time)
        VALUES (#{orderNo}, #{userId}, #{orderType}, #{totalAmount}, #{payAmount},
        #{status}, #{receiverName}, #{receiverPhone}, #{receiverAddress}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateOrder">
        UPDATE orders SET status = #{status}, pay_time = #{payTime},
        ship_time = #{shipTime}, complete_time = #{completeTime}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <select id="findOrderItems" resultMap="OrderItemResultMap">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

    <insert id="insertOrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, product_id, service_id, name, image_url, price, quantity, service_time, create_time)
        VALUES (#{orderId}, #{productId}, #{serviceId}, #{name}, #{imageUrl}, #{price}, #{quantity}, #{serviceTime}, #{createTime})
    </insert>

</mapper>